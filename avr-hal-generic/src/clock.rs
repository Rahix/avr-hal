/// 24 MHz Clock
pub struct MHz24;

/// 20 MHz Clock
pub struct MHz20;

/// 16 MHz Clock
pub struct MHz16;

/// 12 MHz Clock
pub struct MHz12;

/// 8 MHz Clock
pub struct MHz8;

/// 1 MHz Clock
pub struct MHz1;
